name = "lunework"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# For Cloudflare Pages deployment with @cloudflare/next-on-pages
# Build command: npx @cloudflare/next-on-pages
# Output directory: .vercel/output/static

# Environment variables should be set in Cloudflare Dashboard
# or via wrangler secret put commands

# For local development only - set production values in Cloudflare Dashboard
[vars]
NEXT_PUBLIC_API_BASE_URL = "http://localhost:3001"

